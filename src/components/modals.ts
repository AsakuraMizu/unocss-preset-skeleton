import type { StaticShortcutMap } from '@unocss/core';

export const shortcuts: StaticShortcutMap = {
  /* === Modal (helpers) === */
  'w-modal': 'w-full max-w-640px',
  'w-modal-slim': 'w-full max-w-400px',
  'w-modal-wide': 'w-full max-w-80%',
};
