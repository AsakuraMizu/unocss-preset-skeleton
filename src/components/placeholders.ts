import type { StaticShortcutMap } from '@unocss/core';

export const shortcuts: StaticShortcutMap = {
  /* === Base === */
  placeholder: ['bg-surface-300-600-token', 'h-5', /* Theme: Rounded */ 'rounded-token'],
  /* === Shapes === */
  'placeholder-circle': 'bg-surface-300-600-token aspect-square rounded-full',
};
